<UserControl x:Class="DairyManagement.UI.Views.MilkCycleListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Milk Collection Cycles" FontSize="32" FontWeight="Bold" 
                  Foreground="{StaticResource TextPrimaryBrush}" Margin="20,20,20,10"/>

        <Border Grid.Row="1" Background="White" Padding="20,10" Margin="20,0,20,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Record Milk Collection" Style="{StaticResource PrimaryButton}" 
                       Command="{Binding AddMilkCycleCommand}" Margin="0,0,10,0"/>
                
                <CheckBox Content="Show Unsettled Only" IsChecked="{Binding ShowUnsettledOnly}" 
                         VerticalAlignment="Center" Margin="20,0"/>
                
                <Button Content="🔄 Refresh" Style="{StaticResource SecondaryButton}" 
                       Command="{Binding LoadMilkCyclesCommand}" HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" Margin="20,0,20,20" Padding="10">
            <DataGrid ItemsSource="{Binding MilkCycles}" 
                     SelectedItem="{Binding SelectedMilkCycle}"
                     AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Period" Binding="{Binding Period}" Width="200"/>
                    <DataGridTextColumn Header="Milk Amount" Binding="{Binding TotalMilkAmount, StringFormat=₹{0:N2}}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding IsSettled, Converter={StaticResource BoolToActiveConverter}}" Width="100"/>
                    <DataGridTemplateColumn Header="Actions" Width="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Record Transaction" Style="{StaticResource SecondaryButton}" 
                                           Command="{Binding DataContext.RecordTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           Padding="8,4" Margin="0,0,5,0"
                                           IsEnabled="{Binding IsSettled, Converter={StaticResource InverseBoolConverter}}"/>
                                    <Button Content="Settle" Style="{StaticResource PrimaryButton}" 
                                           Command="{Binding DataContext.ViewSettlementCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           Padding="8,4"
                                           IsEnabled="{Binding IsSettled, Converter={StaticResource InverseBoolConverter}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
