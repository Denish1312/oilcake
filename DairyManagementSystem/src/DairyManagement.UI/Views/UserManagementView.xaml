<UserControl x:Class="DairyManagement.UI.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="User Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Create User Form -->
            <Border Grid.Column="0" Background="#f9f9f9" BorderBrush="#ddd" BorderThickness="1" CornerRadius="8" Padding="30" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Create New Staff / Admin" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

                    <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15"/>

                    <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15"/>

                    <TextBlock Text="Role" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox SelectedValue="{Binding NewRole}" SelectedValuePath="Content" Padding="10" Margin="0,0,0,25">
                        <ComboBoxItem Content="Staff" IsSelected="True"/>
                        <ComboBoxItem Content="Admin"/>
                    </ComboBox>

                    <Button Content="CREATE USER" Command="{Binding CreateUserCommand}" 
                            Style="{StaticResource PrimaryButton}" Height="45"/>
                    
                    <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,15,0,0">
                        <ProgressBar IsIndeterminate="True" Height="4"/>
                    </Grid>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="1" Margin="40,0,0,0">
                <TextBlock Text="About Roles" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                <TextBlock Text="• Admin: Full access to users, settings, and database." TextWrapping="Wrap" Margin="0,0,0,10"/>
                <TextBlock Text="• Staff: Can record milk, sales, and settlements but cannot manage users or change system configuration." TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
