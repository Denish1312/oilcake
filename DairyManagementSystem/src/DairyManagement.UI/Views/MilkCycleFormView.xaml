<UserControl x:Class="DairyManagement.UI.Views.MilkCycleFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border Style="{StaticResource Card}" MaxWidth="500" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="New Milk Collection" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

                <Label Content="Customer *"/>
                <ComboBox ItemsSource="{Binding Customers}" 
                         SelectedItem="{Binding SelectedCustomer}"
                         DisplayMemberPath="FullName" Margin="0,0,0,15"/>

                <Label Content="Start Date"/>
                <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,0,15"/>

                <Label Content="End Date"/>
                <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,0,15"/>

                <Label Content="Total Milk Amount (₹) *"/>
                <TextBox Text="{Binding TotalMilkAmount, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" 
                           Command="{Binding CancelCommand}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="Save" Style="{StaticResource PrimaryButton}" 
                           Command="{Binding SaveCommand}" Width="100"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
