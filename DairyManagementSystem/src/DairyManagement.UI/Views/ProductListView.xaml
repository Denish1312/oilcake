<UserControl x:Class="DairyManagement.UI.Views.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Title -->
        <TextBlock Grid.Row="0" Text="Product Management" FontSize="32" FontWeight="Bold" 
                  Foreground="{StaticResource TextPrimaryBrush}" Margin="20,20,20,10"/>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="White" Padding="20,10" Margin="20,0,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="➕ Add Product" Style="{StaticResource PrimaryButton}" 
                       Command="{Binding AddProductCommand}" Margin="0,0,10,0"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <CheckBox Content="Active Only" IsChecked="{Binding ShowActiveOnly}" 
                             VerticalAlignment="Center" Margin="0,0,10,0"/>
                </StackPanel>

                <Button Grid.Column="3" Content="🔄 Refresh" Style="{StaticResource SecondaryButton}" 
                       Command="{Binding LoadProductsCommand}"/>
            </Grid>
        </Border>

        <!-- Product DataGrid -->
        <Border Grid.Row="2" Background="White" Margin="20,0,20,20" Padding="10">
            <DataGrid ItemsSource="{Binding Products}" 
                     SelectedItem="{Binding SelectedProduct}"
                     AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding ProductCode}" Width="100"/>
                    <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="Unit" Binding="{Binding UnitOfMeasure}" Width="80"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding UnitPrice, StringFormat=₹{0:N2}}" Width="100"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding CurrentStock, StringFormat={}{0:N2}}" Width="100"/>
                    <DataGridTextColumn Header="Reorder Level" Binding="{Binding ReorderLevel, StringFormat={}{0:N2}}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding StockStatus}" Width="100"/>
                    <DataGridTemplateColumn Header="Actions" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Style="{StaticResource SecondaryButton}" 
                                           Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           Padding="8,4" Margin="0,0,5,0"/>
                                    <Button Content="Activate" Style="{StaticResource PrimaryButton}" 
                                           Command="{Binding DataContext.ActivateProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           Padding="8,4" Margin="0,0,5,0"
                                           Visibility="{Binding IsActive, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                    <Button Content="Deactivate" Style="{StaticResource SecondaryButton}" 
                                           Command="{Binding DataContext.DeactivateProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           Padding="8,4"
                                           Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
