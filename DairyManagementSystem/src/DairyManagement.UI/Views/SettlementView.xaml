<UserControl x:Class="DairyManagement.UI.Views.SettlementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settlement Preview" FontSize="32" FontWeight="Bold" Margin="20"/>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="20,0,20,20">
            <ScrollViewer>
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding Preview.CustomerName}" FontSize="24" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Preview.Period}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="{StaticResource PrimaryBrush}" CornerRadius="4" Padding="10">
                            <StackPanel>
                                <TextBlock Text="Final Payable" Foreground="White" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Preview.NetPayable, StringFormat=₹{0:N2}}" 
                                          Foreground="White" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Separator Margin="0,20"/>

                    <TextBlock Text="Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ItemsControl ItemsSource="{Binding Preview.Details}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Description}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Amount, StringFormat=₹{0:N2}}" HorizontalAlignment="Right"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Separator Margin="0,20"/>

                    <Label Content="Select Payment Mode"/>
                    <ComboBox ItemsSource="{Binding PaymentModes}" SelectedItem="{Binding PaymentMode}" Width="200" HorizontalAlignment="Left"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" Width="150" Margin="0,0,10,0"/>
            <Button Content="Print Receipt" Style="{StaticResource SecondaryButton}" Command="{Binding PrintReceiptCommand}" Width="150" Margin="0,0,10,0"/>
            <Button Content="Finalize &amp; Pay" Style="{StaticResource PrimaryButton}" Command="{Binding CreateSettlementCommand}" Width="150"/>
        </StackPanel>
    </Grid>
</UserControl>
