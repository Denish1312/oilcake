<UserControl x:Class="DairyManagement.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- Page Title -->
                <TextBlock Text="Dashboard" FontSize="32" FontWeight="Bold" 
                          Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

                <!-- Metrics Cards -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Active Customers Card -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Active Customers" FontSize="14" 
                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding TotalActiveCustomers}" FontSize="36" 
                                      FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Active Products Card -->
                    <Border Grid.Column="1" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Active Products" FontSize="14" 
                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding TotalActiveProducts}" FontSize="36" 
                                      FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Pending Settlements Card -->
                    <Border Grid.Column="2" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Pending Settlements" FontSize="14" 
                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding PendingSettlements}" FontSize="36" 
                                      FontWeight="Bold" Foreground="{StaticResource WarningBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Low Stock Products Card -->
                    <Border Grid.Column="3" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Low Stock Products" FontSize="14" 
                                      Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding LowStockProducts}" FontSize="36" 
                                      FontWeight="Bold" Foreground="{StaticResource ErrorBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Loading Indicator -->
                <TextBlock Text="Loading..." FontSize="18" HorizontalAlignment="Center" 
                          Margin="0,40,0,0" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" FontSize="14" Foreground="{StaticResource ErrorBrush}" 
                          HorizontalAlignment="Center" Margin="0,20,0,0" TextWrapping="Wrap"
                          Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
