<UserControl x:Class="DairyManagement.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Business Reports" FontSize="32" FontWeight="Bold" 
                  Foreground="{StaticResource TextPrimaryBrush}" Margin="20,20,20,10"/>

        <!-- Filters -->
        <Border Grid.Row="1" Background="White" Padding="20,15" Margin="20,0,20,10">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <Label Content="Report Type"/>
                    <ComboBox ItemsSource="{Binding ReportTypes}" SelectedItem="{Binding SelectedReportType}" Width="200"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <Label Content="From Date"/>
                    <DatePicker SelectedDate="{Binding DateFrom}" Width="150"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <Label Content="To Date"/>
                    <DatePicker SelectedDate="{Binding DateTo}" Width="150"/>
                </StackPanel>

                <Button Content="Generate Report" Style="{StaticResource PrimaryButton}" 
                       Command="{Binding GenerateReportCommand}" VerticalAlignment="Bottom" Margin="10,0"/>

                <Button Content="Export" Style="{StaticResource SecondaryButton}" 
                       Command="{Binding ExportCommand}" VerticalAlignment="Bottom"/>
            </StackPanel>
        </Border>

        <!-- Results -->
        <Border Grid.Row="2" Background="White" Margin="20,0,20,20" Padding="10">
            <Grid>
                <DataGrid ItemsSource="{Binding ReportResults}" AutoGenerateColumns="True" IsReadOnly="True">
                    <!-- For production, we would define specific grids for each report type using DataTemplateSelectors -->
                </DataGrid>

                <Border Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="Generating Report..." FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
